# 📌 MyGram — Локальный Instagram Direct Parser / Message Engine

**MyGram** — это автономный локальный инструмент для чтения Instagram Direct, парсинга контактов и сообщений, хранения истории в SQLite, последующей автоматизации общения и интеграции с ИИ.  
Работает **без instagram API**, используя **Selenium + Chrome + cookies**, полностью локально.

Проект можно использовать как:

- архиватор переписок,
- систему анализа Direct,
- движок для бота (ИИ-переписка в реальном времени),
- инструмент мониторинга новых сообщений.

---

# 🚀 Возможности

### ✔ Парсинг контактов Instagram Direct  
- Полный скролл списка  
- Фильтрация «заметок» и системных блоков  
- Сохранение в SQLite (`contacts`)

### ✔ Парсинг сообщений для одного или всех контактов  
- Полный скролл чата до первого сообщения  
- Поддержка всех типов bubble  
- Автоматическое определение отправителя (self / peer)  
- Извлечение текста сообщения

### ✔ Хранение истории сообщений  
База `messages` содержит:

- contact_username  
- sender  
- text  
- order_index (порядок в чате)  
- msg_hash (защита от дублей)  
- timestamp / scraped_at  

### ✔ Поддержка cookies (автоматический вход)  
- При первом запуске логин вручную  
- После этого логин восстанавливается по кукам

### ✔ Инкрементальная синхронизация  
(будет добавлено в следующей версии)

---

# 🧩 Технологии

- Python 3.12  
- Selenium (Chrome WebDriver)  
- BeautifulSoup4  
- SQLite  
- WebDriverManager  

---

# 📁 Структура проекта

```
mygram/
│
├── client/
│   ├── selenium_direct.py            # Основной Selenium-клиент
│   ├── sync_contacts_from_direct.py  # Парсинг контактов
│   ├── sync_messages_for_contact.py  # Парсинг одного контакта
│   ├── sync_messages_for_all.py      # Парсинг всех контактов
│   └── sync_new_messages.py          # (WIP) Инкрементальный парсер новых сообщений
│
├── core/
│   └── models.py                     # Модели ContactSnapshot / MessageSnapshot
│
├── db/
│   ├── connection.py                 # Работа с SQLite
│   ├── contact_repository.py         # Репозиторий контактов
│   └── message_repository.py         # Репозиторий сообщений
│
├── cookies/
│   └── instagram_cookies.json        # (создаётся автоматически)
│
├── init_db.py                        # Инициализация таблиц
├── mygram.db                         # База SQLite
├── README.md
├── services/
│   ├── login_manager.py        # Авто-логин, загрузка/сохранение cookies
│   ├── scroll_engine.py        # Универсальный скроллер (чаты / контакты)
│   └── bubble_parser.py        # Модуль разбора сообщений и типов bubble
```

---

# 🗄 Структура базы данных

## Таблица `contacts`

```
id INTEGER PRIMARY KEY AUTOINCREMENT
username TEXT UNIQUE
display_name TEXT
profile_url TEXT
is_active INTEGER
last_message_preview TEXT
last_message_at_utc TEXT
scraped_at_utc TEXT
```

## Таблица `messages`

```
id INTEGER PRIMARY KEY AUTOINCREMENT
contact_username TEXT
sender TEXT
text TEXT
order_index INTEGER
timestamp_utc TEXT
scraped_at_utc TEXT
msg_hash TEXT UNIQUE(contact_username, msg_hash)
```

---

# ▶ Как запустить проект

## 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

## 2. Инициализация базы

```bash
python -m init_db
```

## 3. Первый запуск — парсинг контактов

```bash
python -m client.sync_contacts_from_direct
```

Процесс:

1. Откроется Chrome  
2. Войти в Instagram вручную  
3. Скрипт сам перейдёт в Direct  
4. Спарсит весь список и сохранит в БД

## 4. Парсинг сообщений (всех контактов)

```bash
python -m client.sync_messages_for_all
```

Chrome откроется, использует cookies, перейдёт в Direct и начнёт обходить диалоги.

## 5. Парсинг сообщений одного пользователя

Отредактируй username в файле:

```
client/sync_messages_for_contact.py
```

И запусти:

```bash
python -m client.sync_messages_for_contact
```

---

# 🛠 Планы на ближайшие обновления

- [x] Скролл контактов и чатов  
- [x] Фильтрация заметок  
- [x] Обновлённый парсер пузырей  
- [ ] Инкрементальная синхронизация новых сообщений  
- [ ] WebUI (просмотр чатов)  
- [ ] Бот-движок: автоматические ответы через ИИ  
- [ ] Авто-ответчик по расписанию  
- [ ] Отправка сообщений через Selenium  
- [ ] Telegram-уведомления о новом сообщении  

---

---

# ❓ FAQ — Часто задаваемые вопросы

### **Почему нужен Selenium, а не API?**
Instagram ограничивает и закрывает публичные API. Selenium даёт полный доступ к Direct без ограничений на чтение истории.

### **Можно ли запускать на сервере?**
Да. Достаточно режима `--headless` в Chrome, однако первый логин всё равно нужно выполнить один раз вручную.

### **Будут ли забанены cookies?**
Cookies Instagram живут 1–30 дней в зависимости от режима безопасности аккаунта. Скрипт автоматически обновляет их при каждом запуске.

---

# ⚠️ Известные ограничения

- Не парсятся аудио/видео (помечаются как unsupported)
- Веб-версия Instagram иногда меняет DOM — требуется обновление селекторов
- При очень больших чатах (10k+ сообщений) скорость падает из-за DOM размеров

---

# 🧠 Архитектура обработки

1. **Selenium-клиент**
   - управляет Chrome
   - открывает Direct
   - выполняет скролл и собирает HTML

2. **Парсер**
   - фильтрует элементы
   - извлекает текст, отправителя, порядок сообщений

3. **Хранилище**
   - SQLite: contacts/messages
   - репозитории выполняют upsert + защиту от дублей

4. **Автоматичeский вход**
   - cookies.json
   - при отсутствии файла создаётся заново

---

# 🩺 Troubleshooting

### Chrome открывается, но не входит в аккаунт
Удалите cookies:
```bash
rm cookies/instagram_cookies.json
```
И выполните вход заново.

### Контакты не парсятся
Иногда Instagram меняет контейнер. Перезапустите Chrome — приложение автоматически найдёт новый fallback-контейнер.

### Сообщения не догружаются до конца
Используйте:
```bash
max_scrolls=50
```
или выше — старые аккаунты требуют много прокрутки.

---

# 📄 License

Проект распространяется под MIT License — свободно используйте, модифицируйте и развивайте.

# 📬 Контакты

Автор проекта — **i4l00t**

📨 Telegram: https://t.me/i4l00t  
🛠 Технические вопросы и баг-репорты: используйте раздел Issues  
📥 Предложения по функционалу: Pull Requests приветствуются